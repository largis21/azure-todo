on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  run_deployment_workflow_production:
    name: Run Deployment Workflow
    runs-on: ubuntu-latest
    steps: 

      - name: Set environment
        id: env
        run: echo "::set-output name=environment::${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}"

      - name: Run Deployment Script
        if: steps.env.outputs.environment == 'production'
        run: ./deploy-azure-static-web-apps.yml
        with:
          environment: production

      - name: Run Deployment Script
        if: steps.env.outputs.environment == 'development'
        run: ./deploy-azure-static-web-apps.yml
        with:
          environment: development
